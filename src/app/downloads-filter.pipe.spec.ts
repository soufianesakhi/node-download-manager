import { DownloadsFilterPipe } from './downloads-filter.pipe';

describe('DownloadsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DownloadsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
